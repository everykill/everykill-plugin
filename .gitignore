.gradle
build
.idea/
.project
.settings/
.classpath
nbactions.xml
nb-configuration.xml
nbproject/
snapshot/
# Never let a delivered archive into the repo
*.zip


# generated reference data - CC BY-NC-SA, never committed. tools/fetch-reference-data.sh
data/

# runelite core source, unpacked for reading. somebody else's code - read it,
# reimplement it, never paste it. tools/unpack-runelite-source.sh
rlsrc/
